# OpenCompanion

Stable releases live on `main`. Current development is on [`nightly`](https://github.com/cartergeoco/opencompanion/tree/nightly).

Publishing a GitHub Release (not a prerelease) fast-forwards `main` to that tag.
